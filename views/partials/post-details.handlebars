<section class="card" class="post" data-post-id="{{id}}">
  <header>
    <h4>{{title}}</h4>
    <p>Posted by
      {{user.username}}
      on
      {{format_date post_date}}
    </p>
  </header>
  <p>{{content}} </p>

</section>